#include <iostream>
int main() {
    char ch = 'q';
    std::cout << ch << std::endl;

    // ascii value for char
    char character = 'A';

    // we use (int)variable_name to do integer value for char datatype
    std::cout << (int)character << std::endl; // 65

    return 0;
}