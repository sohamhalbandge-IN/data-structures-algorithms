#include <iostream>
int main() {
    float radius = 8.0f;

    float PI = 3.1415f;

    float area_circle = PI * (radius * radius);

    std::cout << "The area of circle is " << area_circle << std::endl;

    return 0;
}